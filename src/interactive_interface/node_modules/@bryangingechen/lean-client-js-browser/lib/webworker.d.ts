import { Connection, Event, Transport, TransportError } from 'lean-client-js-core';
import { LeanJsOpts } from './inprocesstypes';
export declare class WebWorkerTransport implements Transport {
    opts: LeanJsOpts;
    constructor(opts: LeanJsOpts);
    connect(): WebWorkerConnection;
}
export declare class WebWorkerConnection implements Connection {
    error: Event<TransportError>;
    jsonMessage: Event<any>;
    alive: boolean;
    worker: Worker;
    constructor(worker: Worker);
    send(msg: any): void;
    dispose(): void;
}
